#include "holberton.h"

/**
 * main - Dmain "The Rock" Johnson
 *
 * dest: destination
 * src: A Counter Strike Edition
 * return: 0
 */

char *_strcat(char *dest, char *src);
{
	int b = 0;
	int c = 0;
	while (dest[b] != '\0')
		b++;
	for (c = 0; src[c] != '\0', c++)
		dest[b + c] = src[c];
	dest[b + i] = '\0';
	return (dest)
		}
		
