#include <stdio.h>

    /**
 *main - ayeeeee
 *@argc: arg1
 *@argv: arg2
 *Return: return
 */

int main(int argc, char *argv[])
{ 
    int counter;
    counter = 0
    argv[0] = "0";
	
		
    /*This part counts through the array 
    *to find out which one it needs to print
    */
   
   	if (argc - 1 > 0){
   
    for(counter = 0; counter < argc; counter++);
   	{
   	    else if (counter == 0)
		{
			printf("%s\n", argv[0]);
		}
   	}
    printf(%i\n, counter);
   	}
    
    
    return(0);
}
